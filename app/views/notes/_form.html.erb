<h3>Add a note to the problem:</h3>
<%= form_for([@problem, @problem.notes.build], remote: true) do |f| %>
    <p>
      <%= f.label :body %><br>
      <%= f.text_area :body %>
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>
